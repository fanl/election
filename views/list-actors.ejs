<!DOCTYPE html>
<html>
  <head>
    <title>Movie Data: Actors</title>
    <link rel="stylesheet" href="/style.css">
  </head>
  <body>
    <a href="/"><h1>Movie Data</h1></a>
    <% if (items.length > 0) { %>
    <ul>
      <% items.forEach(function(item) { %>
      <li><a href="/actors/<%=item._id%>"><%=item.name%></a></li>
      <% }); %>
    </ul>
    <% } else { %>
    <div>No actors found.</div>
    <% } %>
    <div class="control">
      <h2>Search for actors by name</h2>
      <form method="get" action="/actors/">
        <label>Actor name: <input name="name"></label>
        <button>Search</button>
      </form>
    </div>
  </body>
</html>